# LibreOffice UI Manager — LOUIM's own dialogs.
#
# Everything in here talks to the LibreOffice toolkit (com.sun.star.awt), so it
# only ever runs inside the extension. The pure logic these dialogs feed lives
# in ``louim.adapters`` and ``louim.template`` and is unit-tested offline.
