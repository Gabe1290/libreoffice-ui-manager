# GitLab CI/CD - byte-compile, run unit tests, build the .oxt artifact.
build:
  image: python:3.11
  script:
    - python -m compileall -q src tools
    - python -m unittest discover -s tests -v
    - python tools/build.py
  artifacts:
    paths:
      - dist/louim.oxt
    expire_in: 30 days
